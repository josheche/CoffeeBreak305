<%= simple_form_for @order, html: { id: :orderform } do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
  <%= hidden_field_tag(:stripeToken) %>
  <%= hidden_field_tag(:stripeEmail) %>
    <div align="center">
      <div class="container">
          <%= f.input :order_profile_id, :input_html => {:id => "selected_profile_id", :value => "", :class => "hidden"} %>
        <!-- big button logo begins -->
        <div class="form-actions" id="submitorder">
          <%= image_submit_tag("button-logo.svg", class: "bigbutton", id: "submitorder") %>
        </div>
      </div>
    </div>
      <!-- big button logo ends -->

      <!-- order profile dropdown begins -->
      <div class="container">
        <div class="dropup pull-left dropup-pad drop-text-size col-md-4 col-xs-6">
          <button class="dropup-toggle rowling" data-toggle="dropdown">
            Order Profiles<span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
            <% @order_profiles.each do |o| %>
            <li>
              <a class="selected_product" tabindex="-1">
                <%= o['name'] %>
              </a>
            </li>
            <li style="visibility:hidden"><%= o['id'] %></li>
            <% end %>
            <li role="presentation" class="divider"></li>
            <li>
              <a href="/order_profiles">
                Create New Order Profile
              </a>
            </li>
          </ul>
        </div>
      </div>

    <p id="notice"><%= notice %></p>


  </div>
<% end %>
